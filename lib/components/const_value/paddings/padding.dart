import 'package:flutter/material.dart';

const padding6All = EdgeInsets.all(6.0);
const padding6V = EdgeInsets.symmetric(vertical: 6.0);
const padding6H = EdgeInsets.symmetric(horizontal: 6.0);

const padding8All = EdgeInsets.all(8.0);
const padding8Bottom = EdgeInsets.only(bottom: 8.0);
const padding8V = EdgeInsets.symmetric(vertical: 8.0);
const padding8H = EdgeInsets.symmetric(horizontal: 8.0);

const padding12All = EdgeInsets.all(12.0);
const padding12V = EdgeInsets.symmetric(vertical: 12.0);
const padding12H = EdgeInsets.symmetric(horizontal: 12.0);
const padding12Bottom = EdgeInsets.only(bottom: 12.0);
const padding12Top = EdgeInsets.only(top: 12.0);

const padding15All = EdgeInsets.all(15.0);
const padding15V = EdgeInsets.symmetric(vertical: 15.0);
const padding15H = EdgeInsets.symmetric(horizontal: 15.0);
const padding15Bottom = EdgeInsets.only(bottom: 15.0);
const padding15Top = EdgeInsets.only(top: 15.0);

const padding18All = EdgeInsets.all(18.0);
const padding18V = EdgeInsets.symmetric(vertical: 18.0);
const padding18H = EdgeInsets.symmetric(horizontal: 18.0);
const padding18Bottom = EdgeInsets.only(bottom: 18.0);

const padding10All = EdgeInsets.all(10.0);
const padding10V = EdgeInsets.symmetric(vertical: 10.0);
const padding10H = EdgeInsets.symmetric(horizontal: 10.0);
const padding10Bottom = EdgeInsets.only(bottom: 10.0);
const padding10Top = EdgeInsets.only(top: 10.0);

const padding5All = EdgeInsets.all(5.0);
const padding5V = EdgeInsets.symmetric(vertical: 5.0);
const padding5H = EdgeInsets.symmetric(horizontal: 5.0);
const padding5Bottom = EdgeInsets.only(bottom: 5.0);
const padding5Top = EdgeInsets.only(top: 5.0);

const padding25All = EdgeInsets.all(25.0);
const padding25V = EdgeInsets.symmetric(vertical: 25.0);
const padding25H = EdgeInsets.symmetric(horizontal: 25.0);
const padding25Bottom = EdgeInsets.only(bottom: 25.0);
const padding25Top = EdgeInsets.only(top: 25.0);
